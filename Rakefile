#!/usr/bin/env rake
# Add your own tasks in files placed in gravatar-plugin/tasks ending in .rake,
# for example gravatar-plugin/tasks/capistrano.rake, and they will automatically be available to Rake.

require File.expand_path('../config/application', __FILE__)

SampleApp::Application.load_tasks
